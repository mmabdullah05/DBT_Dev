name: 'my_new_project'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# dispatch:
#   macro_namespace: ["dbt_mmabdullah05"]

vars:
  catalog: 'raw'

models:
  my_new_project:
    +pre-hook: "USE CATALOG raw"
    +schema: jaffle_shop
    +catalog: raw
    example:
      +materialized: table